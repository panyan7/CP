#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(false);
    cin.tie(0);
    int cc = 0;
    while (true) {
        cc++;
        int n; cin >> n;
        if (n == 0)
            break;
    }
}
