#include <bits/stdc++.h>
using namespace std;

int t, n, k;
char s[100001];

void solve() {
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> t;
    while (t--) {
        cin >> n >> k;
        for (int i = 1; i <= n; ++i) cin >> s[i];
        solve();
    }
    return 0;
}
