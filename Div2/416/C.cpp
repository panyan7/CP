#include <bits/stdc++.h>
using namespace std;
#define ll long long
#define pii pair<int,int>
#define pll pair<long long,long long>

int tt = 1, n, m, k;
int dp[5005][5005], a[5005];

// check long long
void solve() {
    cin >> n;
    for (int i = 1; i <= n; i++)
        cin >> a[i];
    // dp[i][j] == first i people, no destination j
    
}

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);
    cin >> tt;
    while (tt--) {
        solve();
    }
    return 0;
}
